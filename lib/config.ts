/**
 * Feature flags to control which sections are live vs "coming soon".
 * Set each to `true` to enable the feature, `false` to show coming soon.
 */
export const features = {
  shop: false,
  merch: false,
  repair: false,
} as const
